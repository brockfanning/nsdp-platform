def alter_data(df):
    # Perform any alterations on the Pandas DataFrame "df".
    # For example, to drop the "education" column you could use:
    # df = df.drop(columns=['education'])
    return df
